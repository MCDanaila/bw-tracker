{
  "buildCommand": "npm run build",
  "installCommand": "npm install && cd backend && uv pip install -r requirements.txt && cd ..",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/backend/$1"
    }
  ]
}
